apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mini-app
spec:
  rules:
    - host: mini.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: mini-app
                port:
                  number: 80